---
title: "Zero-Knowledge-Bridge: Auto-Import von Bank-E-Mails"
summary: "Leite Bank-Benachrichtigungs-E-Mails über die Push-API mit einem lokalen Python-Helper direkt in Budgero weiter – keine Programmierkenntnisse erforderlich, keine Bankpasswörter geteilt."
section: integrations
topicId: push-api-email-bridge
badge: "Neu"
order: 20
takeaways:
  - "Installiere Python + VS Code, generiere einen Budgero-Push-API-Token und einen Verschlüsselungsschlüssel und speichere sie in einer lokalen .env-Datei."
  - "Nutze das Starter-Bridge-Skript, um Bank-Benachrichtigungs-E-Mails zu lesen, lass KI einen Parser für deine Bank erstellen und teste sicher im Dry-Run-Modus."
  - "Übertrage verschlüsselte Transaktionen mit dem Python-SDK in Budgero; plane das Skript für die automatische Ausführung ein, sobald es validiert ist."
---

**„Willst du etwas richtig gemacht haben, mach es selbst.“**  
Budgero fragt nicht nach deinem Bankpasswort und übergibt keine Daten an Drittanbieter-Aggregatoren. Stattdessen kannst du Bank-E-Mails direkt auf deinem eigenen Rechner in Budgero einspeisen. Deine Daten fließen: **Bank-E-Mail → Dein Skript → Budgero (verschlüsselt)**. Kein Zwischenmann sieht unverschlüsselte Daten.

Folge dieser anfängerfreundlichen Anleitung (keine Programmierkenntnisse erforderlich). Alles bleibt lokal und privat.

---

## Teil 1: Werkzeuge installieren

1. **Python 3.12+**  
   - Windows: [Python herunterladen](https://www.python.org/downloads/windows/) und **„python.exe zum PATH hinzufügen“** aktivieren.  
   - Mac: [Python herunterladen](https://www.python.org/downloads/macos/).
2. **VS Code**  
   - [VS Code](https://code.visualstudio.com/download) installieren und öffnen.

---

## Teil 2: E-Mail vorbereiten (App-Passwort)

Verwende ein App-Passwort, damit du dein Haupt-E-Mail-Passwort nie speichern musst.

- Gmail: Enable 2-Step Verification → create an **[App Password](https://myaccount.google.com/apppasswords)** (name it “Budgero Bridge”) → copy the 16-character code.  
- Other providers (Outlook/Yahoo/iCloud): search “Generate app password for your provider” and follow their steps.

---

## Teil 3: Dein Projekt einrichten

1. **Erstelle einen Ordner:** `BudgeroBridge` auf deinem Desktop. Öffne ihn in VS Code (Datei → Ordner öffnen).  
2. **Öffne ein Terminal:** VS Code → Terminal → Neues Terminal.
3. **Installiere Bibliotheken:**

```bash
pip install budgero rich pandas python-dotenv imap-tools
```

(Verwende auf dem Mac bei Bedarf `pip3`.)

4. **Erstelle deine Konfigurationsdatei (`.env`)**  
   Das Skript muss wissen, wer du bist und wo die Daten gespeichert werden sollen. Wir verwenden eine `.env`-Datei, um dies sicher zu speichern.
   - Klicke in VS Code auf **New File** und nenne die Datei genau `.env` (nur dot-env).
   - Kopiere den folgenden Text und fülle ihn aus:

```bash
# --- EMAIL SETTINGS ---
EMAIL_ADDRESS="your_email@gmail.com"
EMAIL_APP_PASSWORD="paste_16_char_app_password_here"

# --- BANK FILTER SETTINGS ---
BANK_SENDER_EMAIL="alerts@chase.com"
BANK_SUBJECT_KEYWORD="Transaction Alert"

# --- BUDGERO SECURITY ---
BUDGERO_API_KEY="paste_your_api_token_here"
BUDGERO_ENCRYPTION_KEY="paste_your_space_key_here"

# --- DESTINATION SETTINGS ---
TARGET_BUDGET_ID="1"
TARGET_ACCOUNT_ID="1"
TARGET_CATEGORY_ID="5"
```

**Was diese bedeuten:**

<table>
  <thead>
    <tr>
      <th>Einstellung</th>
      <th>Was du dort eintragen solltest</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code>EMAIL_ADDRESS</code></td>
      <td>Deine vollständige E-Mail-Adresse (z. B. <code>john.doe@gmail.com</code>).</td>
    </tr>
    <tr>
      <td><code>EMAIL_APP_PASSWORD</code></td>
      <td>Das 16-stellige App-Passwort aus Schritt 2 (nicht dein normales Anmeldepasswort).</td>
    </tr>
    <tr>
      <td><code>BANK_SENDER_EMAIL</code></td>
      <td>Öffne einen aktuellen Bankbeleg und kopiere die genaue Absenderadresse (z. B. <code>no-reply@bank.com</code>).</td>
    </tr>
    <tr>
      <td><code>BANK_SUBJECT_KEYWORD</code></td>
      <td>Ein Wort oder eine Phrase, die immer im Betreff steht (z. B. <code>Transaction Alert</code>, <code>Your Receipt</code>, <code>Purchase</code>).</td>
    </tr>
    <tr>
      <td><code>BUDGERO_API_KEY</code></td>
      <td>Dein Push-API-Token aus <strong>Settings → Push API</strong> in Budgero.</td>
    </tr>
    <tr>
      <td><code>BUDGERO_ENCRYPTION_KEY</code></td>
      <td>Dein Space-Verschlüsselungsschlüssel aus <strong>Settings → Push API</strong> in Budgero.</td>
    </tr>
  </tbody>
</table>

📍 **Deine Ziel-IDs finden (einfacher Modus)**  
- Gehe in Budgero zu **Einstellungen → Push API** und scrolle nach unten.  
- Die Tabelle **Deine IDs** listet jedes Budget, Konto und jede Kategorie mit der zugehörigen ID auf.  
- Kopiere die IDs, die du benötigst:
  - `TARGET_BUDGET_ID`: Das Budget, in das du importieren möchtest.  
  - `TARGET_ACCOUNT_ID`: Das Bankkonto (z. B. „Chase Checking“), auf dem diese Transaktionen landen sollen.  
  - `TARGET_CATEGORY_ID` (wichtig): Verwende die ID deiner Kategorie „Nicht kategorisiert“/„Zu budgetieren“ als Startpunkt.

💡 **Pro-Tipp:** Sende alle E-Mail-Importe an deine Kategorie „Nicht kategorisiert“. Erstelle dann Regeln in Budgero (z. B. „Wenn Empfänger Netflix enthält, setze Kategorie auf Abonnements“), damit die App die Sortierung übernimmt. So bleibt das Python-Skript einfach.

> **Halte es geheim, halte es sicher.** Speichere `.env` lokal; teile oder lade es nie hoch.

---

## Teil 4: Füge das Startskript hinzu

Erstelle `bridge.py` und füge dieses gesamte Skript ein:

```python
import os
import imaplib
import email
import re
from datetime import datetime, timedelta
from dotenv import load_dotenv
from rich.console import Console
from rich.table import Table
from rich.panel import Panel
from budgero import BudgeroClient
from budgero.exceptions import APIError, EncryptionError

# --- CONFIGURATION & SAFETY ---
load_dotenv()
console = Console()

# 1. SETUP_MODE: Set to True to generate the AI Prompt. Set False to run the bridge.
SETUP_MODE = False 

# 2. DRY_RUN: If True, prints a table but DOES NOT upload to Budgero.
DRY_RUN = True  

# 3. NUM_SAMPLES: How many emails to send to ChatGPT for training (5 is usually fine, if you get incosistent results you can use more sample so LLM can detect more edge cases).
NUM_SAMPLES = 5

# 4. Lookback in days: How far in the past do you want to search for transaction emails.
LOOKBACK_DAYS =  7

# --- PASTE YOUR AI-GENERATED FUNCTION BELOW THIS LINE ---

def extract_transaction_info(body):
    """
    DEFAULT PLACEHOLDER.
    Run this script in SETUP_MODE = True to generate the code for this function.
    """
    return None, None, None, None, None

# --- END OF PARSING LOGIC ---

def imap_since_days(days: int) -> str:
    d = datetime.today() - timedelta(days=days)
    return d.strftime('%d-%b-%Y')  # IMAP-friendly format


def connect_imap():
    """Helper to connect to Gmail"""
    mail = imaplib.IMAP4_SSL('imap.gmail.com')
    mail.login(os.getenv('EMAIL_ADDRESS'), os.getenv('EMAIL_APP_PASSWORD'))
    mail.select('inbox')
    return mail

def generate_llm_prompt():
    """Fetches real emails and creates a ChatGPT prompt"""
    sender = os.getenv('BANK_SENDER_EMAIL')
    keyword = os.getenv('BANK_SUBJECT_KEYWORD')
    
    console.print(f"[yellow]📡 Connecting to IMAP... Fetching last {NUM_SAMPLES} emails from: {sender}[/yellow]")

    try:
        mail = connect_imap()
        
        # Search for ANY email from sender (Read or Unread) to get samples
        status, messages = mail.search(None, f'(FROM "{sender}" SUBJECT "{keyword}")')
        email_ids = messages[0].split()[-NUM_SAMPLES:] # Get last N
        
        if not email_ids:
            console.print("[red]❌ No emails found! Check your .env sender/keyword.[/red]")
            return

        samples = []
        for num in email_ids:
            _, data = mail.fetch(num, '(RFC822)')
            msg = email.message_from_bytes(data[0][1])
            body = msg.get_payload(decode=True).decode(errors='ignore')
            # Clean up: remove newlines/tabs to save tokens, keep it readable
            clean_body = body[:2500].replace("\r", "").replace("\n", " ") 
            samples.append(f"--- EMAIL SAMPLE ---\n{clean_body}\n")

        mail.logout()

        # Construct the Prompt
        prompt = f"""
I am writing a Python script to parse transaction emails from my bank.
I need you to write a Python function called `extract_transaction_info(body)` that takes the raw HTML/text body of an email and returns the transaction details.

**Important:** You must import `datetime` inside the function or assume `from datetime import datetime` is available.

The Function Signature must be exactly:
`return tx_date, amount, note, tx_type, currency`

Requirements:
1. `tx_date`: **Python datetime object**. Parse the date string found in the email into a real object.
2. `amount`: Float. If format is "1.200,50" (European), convert to standard float 1200.50.
3. `note`: String. The Merchant Name/Payee. Remove HTML tags like &nbsp; or <br>. Clean extra whitespace.
4. `tx_type`: String. "Outflow" or "Inflow".
5. `currency`: String (USD, EUR, RSD, etc).
6. Return `None, None, None, None, None` if the email is not a transaction receipt.

Here are {len(samples)} real samples of my bank emails:
{ "".join(samples) }
        """

        console.print(Panel.fit("✅ SAMPLES FETCHED! COPY THE TEXT BELOW INTO CHATGPT:", border_style="green"))
        print(prompt) 
        console.print(Panel.fit("AFTER CHATGPT REPLIES: Copy the code it gives you and replace the `extract_transaction_info` function in this script.", border_style="blue"))

    except Exception as e:
        console.print(f"[red]Connection Error: {e}[/red]")


def run_bridge():
    """The Main Loop"""
    sender = os.getenv('BANK_SENDER_EMAIL')
    keyword = os.getenv('BANK_SUBJECT_KEYWORD')
    
    console.print(f"[green]🚀 Starting Budgero Bridge for {sender}...[/green]")
    if DRY_RUN:
        console.print("[bold yellow]⚠️ DRY RUN MODE ACTIVE: No data will be uploaded.[/bold yellow]")

    mail = connect_imap()
    since = imap_since_days(LOOKBACK_DAYS)  # last 7 days

    
    # Fetch UNREAD only for the actual run
    status, messages = mail.search(
        None,
        'FROM', f'"{sender}"',
        'SUBJECT', f'"{keyword}"',
        'SINCE', since,
    )
    email_ids = messages[0].split()

    found_transactions = []

    # 1. PARSE STAGE
    for num in email_ids:
        _, data = mail.fetch(num, '(RFC822)')
        msg = email.message_from_bytes(data[0][1])
        body = msg.get_payload(decode=True).decode(errors='ignore')

        # --- CALL THE AI GENERATED FUNCTION ---
        try:
            tx_date, amount, note, tx_type, currency = extract_transaction_info(body)
            
            if amount and tx_date:
                found_transactions.append({
                    "date": tx_date,
                    "amount": amount,
                    "note": note,
                    "type": tx_type,
                    "currency": currency,
                    "id": num # Keep email ID to mark as read later if needed
                })
        except Exception as e:
             console.print(f"[red]Parsing Error on email {num.decode()}: {e}[/red]")

    mail.logout()

    if not found_transactions:
        console.print("[blue]No new transactions found.[/blue]")
        return

    # 2. DISPLAY STAGE (The "Nice Table")
    table = Table(title=f"📥 Found {len(found_transactions)} New Transactions")
    table.add_column("Date", style="cyan")
    table.add_column("Amount", style="magenta", justify="right")
    table.add_column("Currency", style="blue")
    table.add_column("Payee / Note", style="green")
    table.add_column("Type", style="yellow")

    for tx in found_transactions:
        table.add_row(
            tx["date"].strftime("%Y-%m-%d"),
            f"{tx['amount']:.2f}",
            tx["currency"],
            tx["note"],
            tx["type"]
        )
    
    console.print(table)

    # 3. UPLOAD STAGE
    if DRY_RUN:
        console.print("\n[bold yellow]✋ Dry Run Complete. Set DRY_RUN = False to upload.[/bold yellow]")
        return

    console.print("\n[bold green]🚀 Uploading to Budgero...[/bold green]")
    
    client = BudgeroClient(
        api_key=os.getenv('BUDGERO_API_KEY'), 
        encryption_key=os.getenv('BUDGERO_ENCRYPTION_KEY')
    )

    success_count = 0
    for tx in found_transactions:
        try:
            client.add_transaction(
                account_id=int(os.getenv('TARGET_ACCOUNT_ID')),
                category_id=int(os.getenv('TARGET_CATEGORY_ID')),
                budget_id=int(os.getenv('TARGET_BUDGET_ID')),
                date=tx["date"],
                outflow=float(tx["amount"]) if tx["type"] == "Outflow" else 0,
                inflow=float(tx["amount"]) if tx["type"] == "Inflow" else 0,
                memo=tx["note"],
                payee=tx["note"]
            )
            success_count += 1
            # Optional: Mark email as read on server after success
            # mail = connect_imap()
            # mail.store(tx['id'], '+FLAGS', '\\Seen')
            # mail.logout()
        except Exception as e:
            console.print(f"[red]❌ Upload Error for {tx['note']}: {e}[/red]")
    
    console.print(f"[blue]✨ Sync Complete. Successfully imported {success_count} transactions.[/blue]")

if __name__ == '__main__':
    if SETUP_MODE:
        generate_llm_prompt()
    else:
        run_bridge()
```

---

## Teil 5: Bringe dem Skript dein Bankformat bei

1. Öffne `bridge.py` und setze `SETUP_MODE = True`.  
2. Führe `python bridge.py` aus (oder `python3 bridge.py`).  
3. Das Skript gibt einen KI-Prompt mit Beispiel-E-Mails aus. Kopiere ihn.  
4. Füge ihn in ChatGPT/Claude/Gemini ein. Bitte die KI, `extract_transaction_info(body)` gemäß dem Prompt zu schreiben.  
5. Ersetze die Platzhalter-Funktion `extract_transaction_info` in `bridge.py` durch die Version der KI.  
6. Setze `SETUP_MODE = False` und speichere.

---

## Teil 6: Im Dry-Run testen, dann live gehen

1. Stelle sicher, dass `DRY_RUN = True`. Führe `python bridge.py` aus.  
2. Überprüfe die Tabellenausgabe. Wenn Beträge und Daten korrekt aussehen, setze `DRY_RUN = False` und führe es erneut aus, um in Budgero zu übertragen.  
3. Nicht korrekt? Führe das Setup mit mehr Beispielen erneut aus, indem du `NUM_SAMPLES = 10` vor `SETUP_MODE = True` setzt.

---

## Täglich automatisieren

- **Windows:** Verwende die Aufgabenplanung, um `python bridge.py` einmal täglich auszuführen.  
- **Mac/Linux:** Verwende `cron` (z. B. `0 7 * * * /usr/bin/python3 /path/to/bridge.py`).  
- Lass DRY_RUN ausgeschaltet, sobald du dem Parser vertraust.

---

## Sicherheits-Checkliste

- Behalte `.env` lokal; commite oder teile es niemals.  
- Wechsle dein E-Mail-App-Passwort und deinen Budgero Push API Token, wenn du jemals eine Kompromittierung vermutest.  
- Verwende HTTPS (Standard), damit Tokens beim Übertragen geschützt bleiben.  
- Wenn Budgero eine 409 mit `message_id` zurückgibt, bedeutet das, dass ein Wiederholungsversuch als Duplikat erkannt wurde – kein doppelter Eintrag.  
- Wenn du die Bank oder das Format änderst, führe das Setup erneut aus, um den Parser zu aktualisieren.

Du hast nun eine Zero-Knowledge-Brücke: Deine Bank teilt niemals Passwörter, deine Daten bleiben verschlüsselt und Budgero empfängt Transaktionen automatisch. Viel Spaß mit der datenschutzfreundlichen Automatisierung.
